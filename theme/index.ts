/**
 * Lingua design tokens — barrel export.
 * Import from `@/theme` for typed access to colors, fonts, and the
 * type scale. For className-based styling, prefer the Tailwind/NativeWind
 * utilities generated from the same tokens in `global.css`.
 */
export { colors } from "./colors";
export type { ColorToken } from "./colors";
export { fonts } from "./fonts";
export type { FontFamily } from "./fonts";
export { typography } from "./typography";
export type { TypographyVariant } from "./typography";
